services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fleet
      POSTGRES_PASSWORD: fleet
      POSTGRES_DB: fleet
    ports:
      - '5432:5432'
    volumes:
      - fleet_pg_data:/var/lib/postgresql/data

volumes:
  fleet_pg_data:
